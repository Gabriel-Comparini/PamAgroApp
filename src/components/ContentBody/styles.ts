import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        paddingInline: 15,
        paddingBottom: 25,
        paddingTop: 15,
        flex: 1,
        backgroundColor: "#fff"
    }
});