import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        paddingInline: 15,
        paddingBottom: 50,
        paddingTop: 25,
        flex: 1,
        backgroundColor: "#fff"
    }
});